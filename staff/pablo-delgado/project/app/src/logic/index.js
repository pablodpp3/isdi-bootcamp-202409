import {
    registerUser,
    loginUser,
    getUserName,
    isUserLoggedIn,
    isUserProvider,
    isUserCustomer,
    getUserId,
    logoutUser
} from './users'

const logic = {
    registerUser,
    loginUser,
    getUserName,
    isUserLoggedIn,
    isUserProvider,
    isUserCustomer,
    getUserId,
    logoutUser
}

export default logic